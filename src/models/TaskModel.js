export class Task {
    constructor(id, title, listName, completed, notes) {
      this.id = id;
      this.title = title;
      this.listName = listName;
      this.completed = completed;
      this.notes = notes;
    }
  }